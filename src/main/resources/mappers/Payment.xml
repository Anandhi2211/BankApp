<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.solvd.bankapp.persistence.PaymentDAO">

    <insert id="create" keyColumn="company_account_number" keyProperty="companyAccountNumber" useGeneratedKeys="true" >
        Insert into payments (company_account_number, company_name, bill_amount, logincredentials_username, transactions_transaction_id, customers_ssn)
        values (#{companyAccountNumber}, #{companyName}, #{billAmount}, #{username}, #{transactionId}, #{ssn} )
    </insert>

    <select id="findById">
        Select * From payments
        where company_account_number = #{companyAccountNumber}
    </select>

    <select id="getAll" >
        SELECT * FROM payments;
    </select>

</mapper>